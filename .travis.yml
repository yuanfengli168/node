# sudo: required
services:
  - docker

script:
  - docker build -t mannydesigns/node .
  - docker images mannydesigns/node

before_deploy:
  - docker login -u yuanfengli168 -password-stdin 19940326daxia

deploy:
  provider: script
  cleanup: true
  script: docker push mannydesigns/node
  on:
    branch: master
  # edge: true
